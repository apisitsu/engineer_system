# 4866 · TSG-300W/ZNC CHUTE COVER — formula vs workbook

**Workbook:** `20170508_TOOLING LIST_TSG300(FACE GRIND)_SEIBU` (same file as `4556.md`).
**Calc block form:** pasted picture on the `CHUTE (一体型)` sheet — `4866-14` families, ~230 shelf rows.
**Config machine:** `TSG-300W` (family `4866`) · process 1021/1031 FACE GRIND

---

## CHUTE COVER (4866-14)

Workbook block:
```
OD    = ワーク外径ノミナル (中間)     ODMAX = ワーク外径MAX (中間)
W     = ワーク巾ノミナル (中間)       W(MAX) = ワーク巾MAX (中間)

A : ODMAX + 0.2
B : W(MAX) + 0.1
C : B + 5   (小数点第一位切上げ → in practice bucketed to the next 5 mm: 15/20/25/30/35/40/50)
D : 12 + A/2   (round to 1 dp)
E : ✓ φ10.5×90°  (when B < 25)
F : 2-φ4.5 THRU.
G : C − B (MIN)
```

| out | workbook | config (`tooling_formula`) | verdict |
|---|---|---|---|
| A | `ODMAX + 0.2` | `if(odBf>0, odBf_max, odAft_max) + 0.2` | **exact match** |
| B | `W(MAX) + 0.1` | `if(wBf>0, wBf_max, wAft_max) + 0.1` | **exact match** |
| C | `B + 5`, then **up to the next 5 mm** (shelf C column is all multiples of 5; B 22.38 → C 30) | `ceil(B + 5)` → next **integer** (B 22.38 → 28) | **MISMATCH** — config rounds to the next integer, workbook to the next 5. Should be `ceil05`-style ×5 bucketing (`ceilN((B+5)/5,0)*5`). **Rank/impact: none** — C is not in `tooling_search_rule` (only A, B match). |
| D | `12 + A/2`, round 1 dp — sample A 31.27 → D 27.6 | `roundN(12 + A/2, 1)` | **match** |
| E/F/G | fixed-geometry outputs | not computed | fine — not selectors |

Search rules: A→`dim_a` `tol_minus 0` (match), B→`dim_b` (match). C/D computed but unused.

**CHUTE COVER verdict: A/B/D MATCH the workbook.** **C is wrong** (integer ceil vs next-5-mm bucket) but
carries no weight — it is not a search dimension, so no sheet is affected today. Fix if C is ever wired
into ranking. Balled-inner HAMAI/SEIBU split (audit A-1) is a separate, unencodable concern.
