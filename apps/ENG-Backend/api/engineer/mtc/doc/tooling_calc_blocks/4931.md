# 4931 · KS-400B6 — formula vs workbook

**Workbook:** `（計算式追加予定_山本）20240912_TOOLING LIST_KS-400B6` (`研磨_球研・溝研_4931-XX_KS-400B6（阿部）`)
**Calc block form:** **none.** The `MASTER` sheet is a per-P/N master list — header row:
`P/N · 外径 OD · 巾 Width · 内径 ID · DRIVER · LOADING CHUTE · PILOT PIN · … · WORK GUIDE · FRONT SHOE ·
REAR SHOE · PUSHER GUIDE · WORK PUSHER · … · STOCKER CHUTE · … · ROTARY DRESSER`. The 33 shapes on the
sheet are comments/textboxes, not a formula image. **The filename itself says 「計算式追加予定」 — "formula
to be added".**
**Config machine:** `KS-400B6` · 球研 (sphere) + **溝研 (groove)** grind

---

## Config has formulas; the workbook has no block to check them against

| tooling | config formula | provenance |
|---|---|---|
| FRONT SHOE | `if(isBallInner, odBf*0.32, W/2 + 2)` | ported / adapted from KS-400B1 family |
| REAR SHOE | `if(isBallInner, odBf − 0.5, W/2 + 2)` | ″ |
| WORK PUSHER | `odBf * 0.55` | ″ |
| WORK DRIVER | `idBf − 0.4` | ″ |
| WORK GUIDE | `round(W)` | ″ |
| LOADING CHUTE | `C = odBf + 0.15 · D = W + 0.1 · B = 30/40 (W<20) · E = 10/20 (idBf<7)` | ″ |
| PILOT PIN | `A = ID_min − 1 · B = W + 3.5 · …` | shared with KS-400B1 (`PILOT PIN ASSY … 設計は 4931-XX で行うこと` per the master sheet) |
| PLUG | `A = ID_min − 1 · B = W − 1.5 · C = min(W×0.6, 8)` | ″ |

---

## Verdict — NOT VERIFIABLE against this workbook / weakly validated

- **No calc block exists** in the workbook (`計算式追加予定`), so a formula-vs-workbook diff is not possible
  for KS-400B6. The config's 8 tooling formulas were ported from KS-400B1's sphere-grind set.
- Where the audit **measured** one against the factory plan — **STOCKER CHUTE**, copying the KS-400B1
  formula (`A = odBf_max + 0.5`, ±1.0) to KS-400B6's process 1161 — it scored **17 % top-2** (against
  95 % on KS-400B1). Audit B-3: the 1161 side needs its own rule and does not have a documented source.
- The **溝研 (groove-grind)** side specifically has no rule at all (audit B-3/F-3; families `4832`/`4837`
  on KN-312 are the analogous unencoded groove-grind gap).

**This is the conformance page's NOTE flag, confirmed:** `state = rule` is technically true (formula +
search-rule rows exist), but the formulas are unvalidated ports, the workbook offers nothing to check
them against, and the one measured case failed. Getting a real rule here needs the workbook's
`計算式` to be filled in (its own filename says so) — a Yamamoto TODO.
