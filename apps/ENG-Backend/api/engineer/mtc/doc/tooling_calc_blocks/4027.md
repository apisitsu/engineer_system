# 4027 · KS-B22G — formula vs workbook

**Workbook:** `20150609_TOOLING LIST_KS-B22G.xls` (`研磨_内研_4027-XX_KS-B22G(坂本)`)
**Calc block form:** pasted picture on the `JAW` / `BACK PLATE` sheets (not cell text). PDFs in this folder.
**Config machine:** `KS-B22G` · process `1061` ID GRIND / `0351` FINISH ID

---

## JAW (4027-01)

| | workbook (`JAW.pdf` image) | config (`tooling_formula`) | verdict |
|---|---|---|---|
| A | `OD` — ワーク球径 (Tolerance Center) ※Check Process Before/After Grind | `if(isODtoID, odAft_max, if(odBf>0, odBf_max, odAft_max))` | **match** — "OD" with the ※-note (before/after) encoded via `isODtoID` + `_max` |
| B | `A − 0.4` | `A - 0.4` | **match** (exact) |
| C | `18.5 + W/2 + 3` (STANDARD 通常設計) / `18.5 + W − 2` (ABR・ボール入りインナー) · ※round up 0.5 jump | `ceil05(18.5 + W/2 + 3)` · `ceil05(18.5 + W - 2)` `[if isABR]` | **match** — `isABR` = the ABR/balled-inner branch; `ceil05` = 整数または0.5飛び |
| D | `10` (Under 10 Accept — 小径ワーク・ボール入り…) | `D = 10` | **match** |

**JAW verdict: MATCH.** Config faithfully encodes the workbook calc block.

---

## BACK PLATE (4027-02)

| | workbook (`BACK_PLATE.pdf` image) | config (`tooling_formula`) | verdict |
|---|---|---|---|
| A | `ID + 0.3` — ワーク内径研磨後 (MAX) | `idAft_max + 0.3` | **match** (exact) |
| B | `A + 1.0` (Round up x.x) | `ceilN(A + 1, 1)` | **match** — `ceilN(…,1)` = round up 1 dp |
| C | `2 ~ 4.5` | — (not in config) | **OK** — a spec range, not a selector; correctly omitted |
| D | `28 ~ 36` | — (not in config) | **OK** — spec range, not a selector |

Shelf columns are `A MIN | B MAX` (containment). Search rules: A→`dim_a` (`tol_minus 0`, match), B→`dim_b` (match). Reasonable encoding of `A MIN ≤ value ≤ B MAX`.

**BACK PLATE verdict: MATCH.**

---

## Machine limits — two DELIBERATE divergences (evidence-backed, not defects)

The workbook's `Machine Limit` box on `BACK_PLATE.pdf` reads **`OD:Φ38MAX · ID:Φ4.8≦ID<Φ16 · W:14 MIN`**.

| bound | workbook | config `tooling_machine_limit` | why it differs |
|---|---|---|---|
| OD | ≤ 38 | ≤ 38 | same |
| W | ≥ **14** | ≥ **5** | factory plan override — `20260815o_`: 97 of 161 planned parts sit between W 5–14, none below 5. RE33042 A §7 lags practice. Audit doc §KS-B22G. |
| ID | 4.8 – **16** | 4.0 – **19.7** | `20260821d_` — the ¾" (19.05) JAW family: 22 planned C/N were being cut off. Audit D-2. |

**Family verdict: formulas MATCH the workbook; two machine-limits diverge on purpose with recorded plan evidence.**
