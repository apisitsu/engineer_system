# 4651 · LNC45/C200 (FINISH ID) BODY HOLDER — formula vs workbook

**Workbook:** `20210916_TOOLING LIST_薄型ボディホルダー` (`切削_内径_4651-20_薄型ボディホルダー(榊原)`)
**Calc block form:** the `DIMENSION` sheet is **C/N-driven** (「CNのみ直接入力のこと」), like the X-100 ARBOR
sheet. `DIMENSION.pdf` / `XXX_OLD_DIMENSION.pdf` vendored.
**Config machine:** `LNC45/C200` (`inventory_table = tooling_finish_id`) · process 0351 FINISH ID
**Standard:** `RE33024 D` governs the arithmetic. The `standard` column in TEMPLATE_B prints the phantom
`RE33033`; the family-level standard it means is **`RE33028`** ("ボディ内研用治具", vendored 2026-08-28) —
see `4651-20` note in `api/engineer/mtc/doc/tooling_select_rules_DRAFT.md §9`.

---

## BODY HOLDER (4651-20)

Full sheet block (from `20260815c_body_holder_finish_id.js` header — transcribed with the workbook open):
```
A = ROUND(HD, 2)                     頭部受け径  (head receive dia)
B = ROUND(CD + 0.5, 1)               内径
C = ROUND(10 + 巾 × 0.75, 0)          厚さ        (thickness)
D = FLOOR(FL − C/2 ∓ 1, 0.5), cap 28  受け面長さ
E = D + 5  when D < 25                Dカット高さ
```

| out | workbook | config | verdict |
|---|---|---|---|
| A | `ROUND(HD, 2)` | `roundN(HD, 2)` (`HD` = `blank_head`, scored **73/73 = 100 %**) | **exact match** |
| C | `ROUND(10 + 巾 × 0.75, 0)` | `roundN(10 + headWidth * 0.75, 0)` (`headWidth` = `head_width`, scored **81/81 = 98 %**) | **exact match** |
| B | `ROUND(CD + 0.5, 1)` | — **not shipped** | `CD` (面取り径) has no spec column above 50 % — cannot supply it |
| D / E | need `FL` (フラット部長さ) | — **not shipped** | `FL` has no spec column above 50 % |

**Deliberate partial** — A and C ship (the two the spec context can support, both column-mapped by
measurement); B/D/E cannot until CD/FL exist in the spec table. Same call as X-100 ARBOR's B filter.

## Machine limit

`HD ≤ 68` — `RE33024 D §7 対象ワークサイズ HD ≤ φ68`, verified live (a CN with HD 70.48 is excluded).
The only eligibility limit any 2026-08-14+ machine added.

**Family verdict: A and C = EXACT MATCH to the sheet; B/D/E intentionally omitted (missing spec inputs),
recorded. Limit sourced to RE33024 D.**
