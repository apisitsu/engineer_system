# 4033 · KS-500RD — formula vs workbook

**Workbook:** `KS-500RD_TOOLING LIST 20181011_IDY` (`研磨_球研_4033-XX_KS-500RD(井出)`)
**Calc block form:** the workbook sheets are **computed per-P/N tables** (each row already carries the
tooling dims), not a formula block. `A`/`B`/`A–H` columns per part; header names the driving variable
(`SDに依存`, `IDに依存`, `WIDHに依存`).
**Config machine:** `KS-500RD` · process — OD GRIND (球研)

---

## WORK DRIVER (4033-01) — `SDに依存`

Columns: P/N · … · **SD 肩径** · DWG · **A** · **B**

| | workbook | config (`tooling_formula`) | verdict |
|---|---|---|---|
| A | `SD − 0.2` (e.g. SD 19.38 → 19.18 · SD 41.00 → 40.80) | `A = SD - 0.2` | **exact match** — every sampled row |
| B | `A − 7` (e.g. 19.18 → 12.18) | `B = A - 7` | **exact match** |

Search rules: A→`dim_a` ±0.6 (match), B→`dim_b` (rank).

**WORK DRIVER verdict: MATCH.** (Audit G-3: formula right; the thin shelf coverage was fixed separately
with a 19-row cn-map.)

---

## LOADING PINTLE (4033-02) — `IDに依存`

Columns: P/N · … · ID After · OD · WIDH · SD · DWG · **A B C D E F G H**

| out | workbook (from the per-P/N table) | config | verdict |
|---|---|---|---|
| A | `ROUND(ID − 1, 1)` — ID 22.225 → **21.2** | `round(ID - 1)` → **integer** (21) | **near** — 1-dp vs integer; within A's ±1.5 match tol, but should be `roundN(ID - 1, 1)` |
| B | `ROUND(ID + 3, 1)` — ID 22.225 → 25.2 | `round(ID + 3)` → integer | **near** — same 1-dp issue (rank-only) |
| C | step: WIDH 19.05 → **11**, ≥20 → **12** | `if(wAft_max <= 20, wAft_max * 0.6, 12)` → 19.05×0.6 = 11.43 | **near** — approximation of an integer step (rank-only) |
| D | ID ≤ ~24.5 → 9.5 · > 24.5 → 17.5 | `if(ID<=14.5, 9, if(ID<=24.5, 9.5, 17.5))` | **match** (the `ID≤14.5→9` branch has no rows to check) |
| E | ID ≤ 24.5 → 5.5 · > 24.5 → 11 | `if(ID<=24.5, 5.5, 11)` | **match** |
| F | `ROUND(ID − 4.5, 1)` — ID 16 → 11.5 | `round(ID - 4.5, 1)` | **match** |
| G | ID ≤ ~24.5 → **10** · > 24.5 → 20 (`9` only on the old S00178 / `4033-02-0011/12` pintles) | `if(ID<=24.5, 9, 20)` | **MISMATCH** — small branch is `9`, workbook current design is `10` (rank-only, `is_match_dim=false`) |
| H | `ROUND(ID − 0.8, 1)` — ID 16 → 15.2 | `round(ID - 0.8, 1)` | **match** |

**LOADING PINTLE verdict: mostly MATCH.** A/B lack the `, 1` rounding (minor); **G's small-band value
is `9` where the current workbook design is `10`** — rank-only so low impact, but a real formula error.
Audit G-1 measured 89 % top-2.

---

## FRONT SHOE (4033-03) — `WIDHに依存`

Config sources this from a **different file** — `20180821_KS-500RD_投入型式シュー対応表.xlsx` — not the
tooling list. `A` = a step function of **W** (`6≤W<12→0`, `12≤W<20→19`, `20≤W<28→21`, `28≤W<36→28`,
`36≤W<44→37`, `44≤W<64→46`). The `FRONT_SHOE.tsv` in this workbook is the drawing list and confirms the
**W(WIDH)-dependency** (`WIDHに依存` header). Audit G-2 found and validated this rule (16 % → 68 % top-2
after switching from OD to W). Search rule A→`dim_a` ±2.0.

**FRONT SHOE verdict: MATCH to the cited source** (`対応表`); exact step boundaries not re-checked here
(that file was not extracted). W-dependency confirmed by this workbook.

---

## Machine limits

| bound | config `tooling_machine_limit` | note |
|---|---|---|
| ID | `14 – 38.125` | **no `description` — provenance lost** (audit G-4, still open) |
| OD | `26 – 59.531` | **no `description`** — same |

**Family verdict: WORK DRIVER = exact MATCH; LOADING PINTLE ≈ MATCH (A/B rounding, G value 9 vs 10);
FRONT SHOE = MATCH to its cited `対応表`; limits have no recorded source.**
