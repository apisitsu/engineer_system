# 4556 · TSG-300W CARRIER — formula vs workbook

**Workbook:** `20170508_TOOLING LIST_TSG300(FACE GRIND)_SEIBU` (`研磨_巾研_4556・4866-XX_西部巾研機TSG300(百瀬)`)
**Calc block form:** pasted picture on `CARRIER` sheet. (4866 CHUTE COVER shares this workbook → `4866.md`.)
**Config machine:** `TSG-300W` (also serves `TSG-300ZNC` via group) · process 1021/1031 FACE GRIND

---

## CARRIER (4556-01)

Workbook `設計基準` block:
```
A = ワーク外径 + (0.5～1.0)
B = 404 − A
C = (400 + A − B) / 2
```

| out | workbook | config (`tooling_formula`) | verdict |
|---|---|---|---|
| A | `OD + (0.5–1.0)` — sample OD 31.070 → A min 32.0 | `ceil(if(odBf>0, odBf_max, odAft_max) + 0.5)` → 31.070 + 0.5 → ⌈32.0 | **match** — `+0.5 then ceil` lands inside the workbook's 0.5–1.0 range |
| B | `404 − A` — sample → 372.0 | `404 - A` | **exact match** |
| C | `(400 + A − B) / 2` — sample → 30 | `A - 2` | **exact match** — algebraically identical: with `B = 404 − A`, `(400 + A − B)/2 = (2A − 4)/2 = A − 2` |
| D | `D算出用` step column, sample W 22.23 → D 12 | nested step of `W × 0.55` (≤2.5→2.5 … ≤12→12, else `round(W×0.55)`) → 22.23×0.55 = 12.23 → 12 | **plausible match** — sample checks; the 10-step table (2.5/3/4/4.5/5/6/8/9/10/12 = shim thicknesses) not fully re-verified against `D算出用` |

Search rules: A→`dim_a` `tol_minus 1` (match), B/C→rank, D→`dim_d` `tol_plus 0` (match).

**CARRIER verdict: MATCH.** A/B/C reproduce the `設計基準` block exactly (C via the B-substitution identity);
D is a plausible step lookup.

---

## Machine limits

| bound | workbook | config | note |
|---|---|---|---|
| OD | `※OD MIN φ9.5` (shelf rows below ~11 are marked 使用していない→ラップ盤使用) | `OD 11.10 – 42.0` (`per std_mc_con.md`) | config min slightly tighter than the workbook's φ9.5 — the 9.5–11 rows are "not used" anyway |
| W | `巾MIN 6.0` stated on the sheet | **none** | config has no W bound — consistent with audit A-2's "no W bound" call (plan runs W>7 heavily), though the workbook does state W MIN 6.0 |

**Family verdict: CARRIER formulas MATCH the `設計基準` block. OD limit ≈ (config 11.1 vs workbook 9.5,
immaterial); W MIN 6.0 from the workbook is intentionally not encoded (audit A-2).**
