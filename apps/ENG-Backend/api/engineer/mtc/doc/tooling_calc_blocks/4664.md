# 4664 · KS-400B1 (~B3/B7) — formula vs workbook

**Workbook:** `20210628_TOOLING LIST_KS400B(SPHERICAL GRIND)` (`研磨_球研_4664-XX_KS-400B1~4(百瀬)`)
**Calc block form:** pasted picture on 8 sheets (WORK DRIVER, SUPPORT BLOCK, LOADING CHUTE, PLUG A/B,
STOCKER CHUTE, PILOT PIN, GUIDE PLATE). Audit rounds B-1…B-6.
**Config machine:** `KS-400B1` (group `KS-400B1/B2/B7`) · process 1041 SPHERICAL GRIND
**7 toolings.**

---

## WORK DRIVER (4664-01) — spot-verified against `WORK_DIRVER.pdf`

Workbook block: `TYPE1 SD<φ19.5 · TYPE2 φ19.5≤SD` · `MACHINE LIMIT W 30MAX / OD φ32MAX`
```
A : SD − 0.5   (0.5飛びで切り上げ / round up 0.5 jump)
B : ID − 0.8   (0.5飛びで切り下げ / round down 0.5 jump)
C : 32 (A<13) / 36 (13≤A)
D : 24 (SD<13.5) / 30 (13.5≤SD)
E : 23 (KS-400B~B3) / 24 (KS-400B4)
F : 8  (KS-400B~B3) / 10 (KS-400B4)
```

| out | config | verdict |
|---|---|---|
| A | `ceil05(SD - 0.5)` | **exact** — `ceil05` = round up 0.5 jump |
| B | `floor05(if(idBf>0, idBf, idAft) - 0.8)` | **exact** — `floor05` = round down 0.5 jump. *(the row's `description` text "SD-4.5, integer floor" is stale/wrong — the formula is right)* |
| C | `if(A < 13, 32, 36)` | **exact** |
| D | `if(SD < 13.5, 24, 30)` | **exact** |
| E | `23` (fixed) | **exact** — KS-400B1 is in the B~B3 group |
| F | `8` (fixed) | **exact** |

**WORK DRIVER verdict: EXACT MATCH (6/6).**

---

## SUPPORT BLOCK (4664-03) — spot-verified against `SUPPORT_BLOCK.pdf`

Workbook block (`OD = Turning OD MAX`, `W = After-grind width MAX`):
```
A : 20 + (OD/2 − OD/6)      B : W + 0.3      C : OD − OD/6
D : 30 − OD/2               E : 30 + OD/4    F : R5 (30≤OD) / no R (OD<30)
```
Sample OD 31.07 W 22.21 → A 30.4 · B 22.5 · C 25.9 · D 14.5 · E 37.8 · F 5.

| out | config | verdict |
|---|---|---|
| A | `round(20 + (odAft_max/2 − odAft_max/6), 1)` → 30.4 | **exact** (`OD/2 − OD/6`; desc "20 + OD/3" is the same value) |
| B | `round(wAft_max + 0.3, 1)` → 22.5 | **exact** |
| C | `round(odAft_max * 5/6, 1)` → 25.9 | **exact** — `OD × 5/6 = OD − OD/6` |
| D | `round(30 − odAft_max/2, 1)` → 14.5 | **exact** |
| E | `round(30 + odAft_max/4, 1)` → 37.8 | **exact** |
| F | — not computed | fine — fillet radius, geometry not a selector |

**SUPPORT BLOCK verdict: EXACT MATCH (5/5 + sample row).** Workbook note: SUPPORT BLOCK `4664-03-XXXX`
and LOADING CHUTE `4664-02-XXXX` share the last 4 digits (a numbering convention, not shared formulas).

---

## The other 5 toolings

| tooling | status |
|---|---|
| STOCKER CHUTE | descriptions cite **RE33037 D + the 4664-34 tooling drawing** (`A = 荒径 MAX + 0.5`, `B = W + 0.5`, `C` on B, `D` on SD, `T` = TYPE1 W≤10). Audit implemented `20260814c_` — matched **73/87** rows using the drawing's 荒径 (before-turning MAX) reading. Considered verified by that audit. |
| PLUG(A) / PLUG(B) | formula rows exist but selection is **cn-mapped** (`20260821b_`, audit B-1 — 1,117 map rows). Formulas are ID/SD-derived approximations; the plan pins the real choice. |
| PILOT PIN | borrows the shelf from `tooling_ks400b6` via `inventory_table_override` (audit B-2 — system correct, TEMPLATE_B missing the row). |
| LOADING CHUTE | own formula (`A = ceil(197 − OD/2 + ⌊OD/6⌋)`, `B = ⌈W+6⌉`, `C = W+0.2`, `D = OD+0.2`, `F = C`). Not re-diffed this pass — PDF `LOADING_CHUTE.pdf` in folder. |

## Machine limits

| bound | workbook `MACHINE LIMIT` | config | source |
|---|---|---|---|
| OD | `φ32 MAX` | `≤ 32` | `RE33037 D §7` — **matches the workbook box too** |
| W | `30 MAX` | `≤ 30` | `RE33037 D §7` — matches |

**Family verdict: WORK DRIVER + SUPPORT BLOCK = EXACT MATCH (spot-verified against the pasted images).
Limits match both RE33037 D and the workbook's own MACHINE LIMIT box. STOCKER CHUTE verified via audit
`20260814c_`; PLUG A/B are cn-mapped by design; LOADING CHUTE not re-diffed (PDF vendored).**
