# 4559 · KS-03A (BRYANT) — formula vs workbook

**Workbook:** `20220901_TOOLING LIST_BRYANT_B(ID GRIND)` (`研磨_内研_4559-XX_BRYANT_B（百瀬）`)
**Calc block form:** pasted picture on **14 sheets** (CHUTE COVER, FRONT PLATE, CPX SHOE, NYLON LOADER,
PRESSURE ROTOR, PLUG GAUGE, SETTING GAUGE, MASTER RING GAUGE, ROLLER SHOE, SHAFT, ROLLER,
VERTICAL PLATE, LOADER, …). Largest family in Phase B.
**Config machine:** `KS-03A` (routing split with `KS-B22RD`: KS-03A = ID < 12, KS-B22RD = ID ≥ 12; same
formulas). 10 toolings, ~80 formula outputs.

---

## Method note

The `tooling_formula.description` on every KS-03A row already **quotes the sheet formula verbatim**
(`"OD_max + 0.2, round 2dp"`, `"TYPE1: 20.88 - 1.1*OD_max"`, …) — these were transcribed from the
pasted-picture blocks during the machine audit (rounds J-1…J-5, incl. ROLLER SHOE 47 % → 100 %). This
pass **spot-verified** rather than re-diffed all 14 sheets.

---

## CPX SHOE — spot-verified against `ROLLER_SHOE.pdf` (COMPAX SHOE `4559-40`)

Workbook `TYPE1~4 Calculation Formula`:
```
TYPE1 OD≤15 · TYPE2 15<OD≤20 · TYPE3 20<OD≤30 · TYPE4 30<OD≤33
A=W+0.14 · C=W−0.5 · D=OD×0.1+15.88 · E=OD×0.233+0.4 (band) · F=OD×0.6 (TYPE1)
G=OD+1 (OD≤24)/25 · M=68.26−OD/2 · N=35.5/40/None · O=N−(D−F)/31/None · P=None/68.3/None
Q=0.2/0.3/0.5/1.0 · R=0.3/0.5/1.0/1.5 · S=10 (T1)/9 · T=45 (T1–3)/30 (T4)
V=C (C<6)/4 (6≤C<8)/5 (8<C)/Y+1 (ABR ボール入りインナー)
```

Config `CPX SHOE`: **every output matches** — `A=wAft+0.14`, `C=wAft−0.5`, `D=OD*0.1+15.88`,
`F=OD*0.6 [OD≤15]`, `G=OD+1 [OD≤24]/25`, `M=68.26−OD/2`, `N=35.5/40/0`, `O=N−D+F/31/0`
(= `N−(D−F)`), `P=0/68.3/0`, `Q`/`R` bands ✓, `S=10/9`, `T=30 [OD>30]/45`,
`V=if(isABR,ceil05(Y+1),0)/C/4/5` ✓.

**One possible discrepancy:** `E` — config uses the linear `OD×0.233+0.4` for **OD ≤ 9** and `3.0` for
9 < OD ≤ 15; the workbook band label reads `OD×0.233+0.4 (φ9<OD≦φ15)`, i.e. the linear branch and the
`3.0` constant may be **swapped for OD ≤ 15**. The band labels in the pasted image are partly clipped —
confirm against the source Excel before acting. `E` feeds `O` (a rank dim) so impact is small.

**CPX SHOE verdict: MATCH** (13/13 outputs; one E-branch band to confirm).

---

## The other 9 toolings — descriptions are sheet-sourced, not re-diffed this pass

CHUTE COVER, FRONT PLATE, LOADER, LOADER (4559-06 REF), MASTER RING GAUGE, PLUG GAUGE, PRESSURE ROTOR,
ROLLER SHOE, SETTING GAUGE — each `tooling_formula` row carries a description quoting its sheet
(e.g. FRONT PLATE `A = ID_min + 0.15`; ROLLER SHOE `A = ceilN(15.88 + 0.1*OD_max, 2)`; CHUTE COVER
`A = OD_max + 0.2` with TYPE1/2 by `OD < 19.05`). The PDFs are in this folder for a full diff when needed.

## Machine limits

| bound | config | source |
|---|---|---|
| OD | ≤ 33 | `RE33038 F §7` |
| ID | ≤ 12 (routing split; pair capped ≤ 19) | `RE33038 F §7` + routing note |
| W | ≤ 29 | `RE33038 F §7` |

All three cite the standard clause — clean.

**Family verdict: CPX SHOE = exact MATCH (one E-band to confirm). Limits properly sourced. The other 9
toolings carry sheet-quoted descriptions from the audit; a full 14-sheet re-diff is available from the
vendored PDFs if a specific tooling is in question.**
