# 4918 · MD-V9910WA (MARKING) PALLET — formula vs workbook

**Workbook:** `20180803_TOOLING LIST_ロッドエンドボディ用レーザマーキング治具` (`マーキング_レーザマーキング（坂本）`)
**Calc block form:** no pasted picture — the rule is a **prose note in the `4918-03` sheet header**; the
`4918-02` / `4918-03` sheets are per-P/N shelf lists. (`MASTER` had 33 shapes = comments, no formula image.)
**Config machine:** `MD-V9910WA` · process 3491 laser marking

---

## PALLET (4918-03)

Workbook `4918-03` sheet header: **`W = ワーク最大巾 + GAP`, `GAP 0.05〜0.35`.**

| out | workbook | config | verdict |
|---|---|---|---|
| A | `ワーク最大巾 + GAP (0.05–0.35)` | `wAft_max + 0.2` | **match** — `0.2` is the mid-band of `[0.05, 0.35]` |

Single output. The pallet slot is sized to the work's max width plus a small clearance; config takes the
middle of the stated GAP range. Search rule matches `dim_a` with tolerance to reach either end of the band.

**Family verdict: MATCH.** The workbook states a GAP *range*, not a single value; config picks the
mid-band, which the search tolerance then widens to cover the whole 0.05–0.35 span.

> Related: `20260827c_` removed `4918-10` HIGH PALLET BASE ASSY from the `sds_machine_tool` whitelist
> (2 C/N, 0.9 % of the process) — an SDS-slot cleanup, unrelated to this formula.
