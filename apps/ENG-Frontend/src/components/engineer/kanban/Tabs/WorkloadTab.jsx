import React from 'react';
import WorkloadDashboard from '../Workload/WorkloadDashboard';

const WorkloadTab = ({ theme }) => {
    return (
        <div>
            <WorkloadDashboard theme={theme} />
        </div>
    );
};

export default WorkloadTab;
