npm config set proxy http://lble131:Eng1123456789@proxyth.bp.minebea.local:8080
npm config set https-proxy http://lble131:Eng1123456789@proxyth.bp.minebea.local:8080
npm config set proxy http://lble485:Kpx89164#4@proxyth.bp.minebea.local:8080
npm config set https-proxy http://lble485:Kpx89164#4@proxyth.bp.minebea.local:8080

npm init -y
npm install axios bcryptjs body-parser cors exceljs express express-fileupload express-promise-router formidable fs-extra moment-timezone multer nodemon pdf-lib pdfjs pg pg-hstore pool qrcode read-excel-file sequelize tedious xlsx

การนำโปรเจกต์ที่เป็น Monorepo (React + Node.js) ไปรันบน Ubuntu Server อย่างมืออาชีพ เราจะไม่รัน `npm start` เหมือนตอน Dev ครับ แต่เราจะทำ 3 ขั้นตอนหลักนี้:

1. **Build Frontend:** แปลง React ให้เป็นไฟล์ Static (HTML/CSS/JS)
2. **Process Management:** ใช้ **PM2** รัน Backend ให้ทำงานเบื้องหลังตลอดเวลา
3. **Reverse Proxy:** ใช้ **Nginx** เป็นด่านหน้า เพื่อแจกงานว่า "อันไหนขอหน้าเว็บ ส่งไฟล์ React" หรือ "อันไหนขอข้อมูล ส่งไปหา Node.js"

นี่คือขั้นตอน Step-by-Step ครับ:

---

### ขั้นตอนที่ 1: Build Frontend (ทำที่เครื่อง Windows ของคุณ)

ที่เครื่องของคุณ ให้รันคำสั่งเพื่อแปลงโค้ด React เป็นไฟล์พร้อมใช้งาน:

```bash
# รันคำสั่งนี้ที่ Root ของโปรเจกต์
npm run build --workspace=apps/ENG-Frontend

```

**ผลลัพธ์:**
คุณจะได้โฟลเดอร์ชื่อ `build` โผล่ขึ้นมาใน `apps/ENG-Frontend/build` ข้างในจะมีไฟล์ `index.html` และโฟลเดอร์ `static` นี่คือสิ่งที่เราจะเอาไปวางบน Server ครับ

---

### ขั้นตอนที่ 2: เตรียมเครื่อง Ubuntu Server

SSH เข้าไปที่ Ubuntu แล้วติดตั้งสิ่งที่จำเป็น:

```bash
# 1. อัปเดตเครื่อง
sudo apt update

# 2. ติดตั้ง Node.js (แนะนำใช้ Version LTS)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 3. ติดตั้ง Nginx (Web Server)
sudo apt install nginx

# 4. ติดตั้ง PM2 (ตัวคุม Backend ให้ทำงานตลอดเวลา)
sudo npm install -g pm2

```

---

### ขั้นตอนที่ 3: อัปโหลดไฟล์ขึ้น Server

คุณต้องก๊อปปี้ไฟล์จากเครื่อง Windows ไปที่ Ubuntu (ผ่าน FileZilla หรือ SCP) โดยวางโครงสร้างประมาณนี้ (สมมติเก็บไว้ที่ `/var/www/my-app`):

```text
/var/www/my-app/
├── backend/            <-- ก๊อปปี้โค้ดจาก apps/ENG-Backend มา (ยกเว้น node_modules)
└── frontend-build/     <-- ก๊อปปี้โฟลเดอร์ "build" ที่ได้จากขั้นตอนที่ 1 มา

```

**ที่โฟลเดอร์ Backend บน Server:**

```bash
cd /var/www/my-app/backend
npm install --production  # ลงเฉพาะ Library ที่จำเป็น

```

---

### ขั้นตอนที่ 4: รัน Backend ด้วย PM2

เราจะไม่ใช้ `node server.js` เพราะถ้าปิดจอโปรแกรมจะดับ เราจะใช้ PM2 แทน:

```bash
# เข้าไปที่โฟลเดอร์ backend
cd /var/www/my-app/backend

# สั่งรัน (สมมติไฟล์หลักชื่อ server.js)
pm2 start server.js --name "eng-backend"

# สั่งให้มันจำว่าถ้าเปิดเครื่องใหม่ ให้รันออโต้
pm2 save
pm2 startup

```

*ตอนนี้ Backend จะรันอยู่ที่ Port `2005` (ตามที่คุณตั้งไว้)*

---

### ขั้นตอนที่ 5: ตั้งค่า Nginx (พระเอกของงาน)

เราจะตั้งค่าให้ Nginx เปิด Port 80 (HTTP ปกติ) แล้วจัดการ Routing ให้เรา

1. สร้างไฟล์ Config:
```bash
sudo nano /etc/nginx/sites-available/eng-system

```


2. แปะโค้ดนี้ลงไป:
```nginx
server {
    listen 80;
    server_name your_server_ip_or_domain; # ใส่ IP Server หรือ Domain

    # 1. ถ้าคนเข้าหน้าเว็บปกติ -> ให้ส่งไฟล์ React (Frontend)
    location / {
        root /var/www/my-app/frontend-build; # path ที่เก็บไฟล์ build
        index index.html index.htm;
        try_files $uri $uri/ /index.html; # บรรทัดนี้สำคัญมากสำหรับ React Router!
    }

    # 2. ถ้าคนยิง API (เช่น /api/...) -> ให้ส่งไปหา Node.js (Backend)
    location /api {
        proxy_pass http://localhost:2005; # ส่งต่อให้ Port ของ Backend
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

```


3. เปิดใช้งาน Config:
```bash
# สร้าง Link
sudo ln -s /etc/nginx/sites-available/eng-system /etc/nginx/sites-enabled/

# เช็กว่าเขียนถูกไหม
sudo nginx -t

# รีสตาร์ท Nginx
sudo systemctl restart nginx

```



---

### ✅ ผลลัพธ์ที่ได้

1. ผู้ใช้เข้าเว็บผ่าน `http://IP-Server` -> เจอหน้าเว็บ React ทันที (ไม่ต้องมี :3000)
2. เมื่อ React ยิง API ไปที่ `/api/login` -> Nginx จะส่งต่อให้ Backend ที่ Port 2005 ทำงานเบื้องหลัง

วิธีนี้เป็นวิธีมาตรฐาน (Production Grade) ที่ปลอดภัย เร็ว และจัดการง่ายที่สุดครับ!


Viewed package.json:1-33
Viewed docker-compose.yml:1-30

ถ้าต้องการนำโปรเจกต์ `EngineerSystem` นี้ไปรันบนเครื่องใหม่ที่ยังไม่มีอะไรเลย (From Scratch) จะต้องเตรียมตัวและทำตามขั้นตอนดังนี้ครับ:

### 1. ติดตั้งโปรแกรมพื้นฐาน (Prerequisites)
เครื่องใหม่จะต้องมี Software เหล่านี้ติดตั้งอยู่:
- **Node.js** (แนะนำเวอร์ชัน 18 หรือ 20 LTS)
- **Python** (แนะนำเวอร์ชัน 3.10 ขึ้นไป) เพื่อใช้รันเซอร์วิสเกี่ยวกับ PDF
- **PostgreSQL** (หรือใช้ผ่าน Docker ก็ได้)
- **Git** สำหรับดึง Source code
- **Docker & Docker Compose** (ทางเลือก หากต้องการรันแบบ Container)

### 2. ดึง Source Code และติดตั้ง Dependencies
เนื่องจากโปรเจกต์นี้เป็นโครงสร้างแบบ **NPM Workspace (Monorepo)** การติดตั้งจะทำที่โฟลเดอร์นอกสุด (Root):
```bash
# 1. Clone repository
git clone <url-ของ-repo-นี้>
cd EngineerSystem

# 2. ติดตั้ง Node Modules สำหรับทั้ง Backend และ Frontend ในคำสั่งเดียว
npm install
```

### 3. ติดตั้ง Python Library สำหรับระบบ PDF
ระบบจัดการ PDF (Unlock และ Repair) บน Backend เรียกใช้ Python Script ดังนั้นต้องลงไลบรารีเหล่านี้:
```bash
cd apps/ENG-Backend
pip install pypdf PyMuPDF
```

### 4. การตั้งค่า Environment Variables (.env)
คุณต้องสร้างไฟล์ `.env` ที่โฟลเดอร์ `apps/ENG-Backend/.env` โดยใส่ค่าที่จำเป็นหลักๆ ดังนี้:
```env
# ตั้งค่า Database สำหรับ eng_db (ระบบหลัก) และ factory databases (rodpc, maqdb)
DB_HOST=localhost
DB_PORT=6543
DB_USER=your_user
DB_PASS=your_password
DB_NAME=eng_system

# JWT Secret สำหรับระบบ Auth
JWT_SECRET=your_secret_key_here

# Gmail OAuth (จำเป็นสำหรับระบบส่งอีเมล ECR/MTC)
GMAIL_CLIENT_ID="your_client_id"
GMAIL_CLIENT_SECRET="your_client_secret"
GMAIL_REFRESH_TOKEN="your_refresh_token"
```
*(ถ้ามีไฟล์ `.env.example` อยู่แล้ว ให้ก๊อปปี้แล้วแก้ชื่อเป็น `.env` ได้เลยครับ)*

### 5. การตั้งค่าฝั่ง Frontend (API URL)
ไปที่ไฟล์ `apps/ENG-Frontend/src/constance/constance_prod.js` (และ `constance.js`)
ให้เช็คบรรทัดที่เป็น:
```javascript
export const apiUrl = "http://localhost:2005/";
```
ปรับให้ตรงกับ IP หรือ Localhost ของเครื่องที่รัน Backend

### 6. รันเซิร์ฟเวอร์
**วิธีที่ 1: รันแบบ Development (รันตรงบนเครื่อง)**
เปิด Terminal ที่โฟลเดอร์ Root (`EngineerSystem`) แล้วรัน:
```bash
npm run dev
```
*(คำสั่งนี้จะใช้ `concurrently` เพื่อรันทั้ง Backend พอร์ต 2005 และ Frontend พอร์ต 3000 พร้อมกัน)*

**วิธีที่ 2: รันผ่าน Docker (สำหรับ Production / จำลองเซิร์ฟเวอร์จริง)**
โปรเจกต์นี้มี `docker-compose.yml` เตรียมไว้ให้แล้ว รันคำสั่งนี้ที่โฟลเดอร์ Root:
```bash
docker-compose up -d --build
```
ระบบจะเปิดพอร์ตดังนี้:
- Backend ทำงานผ่าน: `http://localhost:2005`
- Frontend ทำงานผ่าน Nginx: `http://localhost:80`

### 7. ข้อควรระวังเพิ่มเติม
- **ฐานข้อมูล:** อย่าลืม Import Schema/Tables พื้นฐานของระบบลงใน PostgreSQL ก่อนรัน (โดยปกติน่าจะมีไฟล์ SQL อยู่ในโฟลเดอร์ `db_migrations/`)
- **การใช้ Python Command:** ถ้าในเครื่องใหม่ คำสั่ง Python ใช้ชื่อ `python3` แทนที่จะเป็น `python` อาจจะต้องเข้าไปแก้ในโค้ดฝั่ง Backend เล็กน้อย (เช่นในไฟล์ `pdfHubController.js` ที่เราเพิ่งแก้ไป ที่ใช้คำสั่ง `execFile('python', ...)` ครับ)

เพียงทำตามขั้นตอนเหล่านี้ คุณก็จะสามารถนำระบบขึ้นรันบนเครื่องใหม่ได้สมบูรณ์ครับ!


rundll32.exe sysdm.cpl,EditEnvironmentVariables

set HTTP_PROXY=http://lble131:Eng1123456789@proxyth.bp.minebea.local:8080
set HTTPS_PROXY=http://lble131:Eng1123456789@proxyth.bp.minebea.local:8080



C:\Users\lble131\AppData\Local\Programs\Python\Python314\Scripts\;C:\Users\lble131\AppData\Local\Programs\Python\Python314\;%USERPROFILE%\AppData\Local\Microsoft\WindowsApps;;C:\Users\lble131\AppData\Local\Programs\Ollama;C:\Users\lble131\AppData\Local\Programs\Microsoft VS Code\bin;C:\Users\lble131\AppData\Roaming\npm;C:\Users\lble131\AppData\Local\Programs\Antigravity IDE\bin
C:\Users\lble131\AppData\Local\Programs\Python\Python314\Scripts\
C:\Users\lble131\AppData\Local\Programs\Python\Python314\
%USERPROFILE%\AppData\Local\Microsoft\WindowsApps
C:\Users\lble131\AppData\Local\Programs\Microsoft VS Code\bin
C:\Users\lble131\AppData\Roaming\npm
C:\Users\lble131\AppData\Local\Programs\Antigravity IDE\bin

HTTP_PROXY=http://lble131:Eng1123456789@proxyth.bp.minebea.local:8080
HTTPS_PROXY=http://lble131:Eng1123456789@proxyth.bp.minebea.local:8080

TEMP
%USERPROFILE%\AppData\Local\Temp
TMP
%USERPROFILE%\AppData\Local\Temp