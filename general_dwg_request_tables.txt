====================================================
  ตารางฐานข้อมูลที่ใช้ใน General DWG Request
====================================================

── ใช้โดยตรง (tool_req.js) ────────────────────────
1. tr_request       SELECT / INSERT / UPDATE / DELETE
2. tr_workflow      SELECT / INSERT

── Support (eng_mtc_model.js) ──────────────────────
3. wc_code          SELECT  (dropdown Work Center ในฟอร์ม)
4. holidays_date    SELECT  (คำนวณ due date, ข้ามวันหยุด)

====================================================
  ตารางอื่นในไฟล์เดียวกัน แต่ไม่ใช่ DWG Request
====================================================
(อยู่ใน eng_mtc_model.js — เป็น feature แยก)
- tool_dwg_request   (Tooling-specific DWG request ระบบเก่า)
- tooling_inspect    (Tooling inspection)
- tb_tooling_return  (Tooling return)

(อยู่ใน fixtureLogic.js — fixture selection)
- spec_process
- tooling_ksb22g, tooling_ksb80, tooling_tsg300
- tooling_ks03a, tooling_ks400b, tooling_ks500rd
- tooling_ks400b5, tooling_ks400b6

(อยู่ใน tooling_select.js / dynamicLogic.js)
- mtc_selection_rules

(อยู่ใน sds.js — Setup Data Sheet feature แยก)
- setup_sheet, approval, template
- template_excel_mapping, setup_parameter_value

====================================================
  สรุป: ตารางที่ต้องการสำหรับ General DWG Request
====================================================
จำเป็น (ลบไม่ได้):
  tr_request
  tr_workflow
  wc_code
  holidays_date

ไม่เกี่ยว (ลบได้ถ้าไม่ใช้ feature อื่น):
  ตารางที่เหลือทั้งหมดด้านบน
====================================================
